name: "[DEVELOP TO MAIN] PR aberto automaticamente"

on:
  push:
    branches:
      - 'develop'

jobs:
  create-pr:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout do código
        uses: actions/checkout@v6

      - name: Criar Pull Request para Main
        id: create-pr
        uses: actions/github-script@v6
        with:
          script: |
            const { owner, repo } = context.repo;
            const pullRequests = await github.rest.pulls.list({
              owner,
              repo,
              head: 'develop',
              base: 'main',
              state: 'open'
            });

            if (pullRequests.data.length === 0) {
              const newPullRequest = await github.rest.pulls.create({
                owner,
                repo,
                head: 'develop',
                base: 'main',
                title: '[DEVELOP TO MAIN] PR aberto automaticamente',
                body: 'Este Pull Request foi criado automaticamente para integrar as mudanças de develop para main.'
              });
              console.log(`Pull Request para main criado: ${newPullRequest.data.html_url}`);
            } else {
              console.log('Já existe um Pull Request aberto de develop para main. Nenhum novo foi criado.');
            }